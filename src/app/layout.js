import { Urbanist, Nunito } from "next/font/google";
import 'bootstrap/dist/css/bootstrap.min.css';
import 'bootstrap-icons/font/bootstrap-icons.css';
import "slick-carousel/slick/slick.css";
import "./assets/main.css";

const urbanist = Urbanist({
  subsets: ['latin'],
  weight: ['400', '500', '600', '700', '800', '900'],
  variable: '--body-color-font',
});
const nunito = Nunito({
  subsets: ['latin'],
  weight: ['400', '500', '600', '700', '800', '900'],
  variable: '--heading-font',
});

export const metadata = {
  title: {
    absolute: '',
    default: 'GetAppFully - Cross-Platform Mobile App Development',
    template: '%s | GetAppFully - Cross-Platform Mobile App Development',
  },
  description: 'GetAppFully specializes in cross-platform mobile app development. Build native iOS and Android apps with React Native, Flutter, and modern technologies.',
  openGraph: {
    title: 'GetAppFully - Cross-Platform Mobile App Development',
    description: 'Professional cross-platform mobile app development services. Create native iOS and Android apps with React Native, Flutter, and cutting-edge technologies.',
  },
};

export default function RootLayout({ children }) {
  return (
    <html lang="en">
      <head>
        <meta name="author" content="Themeservices" />
        <link rel="icon" href="/favicon.ico" sizes="any" />
      </head>
      <body className={`${urbanist.variable} ${nunito.variable}`}>
        {children}
      </body>
    </html>
  );
}
